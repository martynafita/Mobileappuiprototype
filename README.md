
  # Mobile App UI Prototype

  This is a code bundle for Mobile App UI Prototype. The original project is available at https://www.figma.com/design/tsgG7eIBXzDm8tyRtd5yBB/Mobile-App-UI-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  